[uwsgi]
project = docsign
uid = ubuntu
base = /var/sites

chdir = %(base)/%(project)
home = /var/venvs/%(project)
module = config.wsgi:application

master = true
processes = 5

socket = /run/uwsgi/%(project).sock
chown-socket = %(uid):www-data
chmod-socket = 660
vacuum = true
